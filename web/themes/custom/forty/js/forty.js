/**
 * @file
 * Forty behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.forty = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
